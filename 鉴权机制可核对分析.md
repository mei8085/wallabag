# Wallabag 鉴权机制可核对分析报告

> **文档说明**：本报告严格区分「仓内可直接证实的事实」与「仅能推断的环节」。所有事实均标注代码证据位置，外部 Bundle 实现缺失处明确标注。
>
> **证据优先级**：当多源证据出现差异时，按以下顺序采信：
> 1. **功能测试断言**（最可信，可执行验证）
> 2. **单元测试断言**（次可信，可执行验证）
> 3. **生产代码实现**（配置、实体、控制器、Voter）
> 4. **外部 Bundle 公开文档**
> 5. **行业惯例/经验推断**（最弱，仅作参考）

---

## 零、证据优先级说明

### 采信顺序原则

```
┌─────────────────────────────────────────────────────────┐
│  优先级 1: 功能测试断言                                  │
│  直接调用 API 端点，验证端到端行为                        │
│  示例: DeveloperControllerTest::testCreateToken()        │
├─────────────────────────────────────────────────────────┤
│  优先级 2: 单元测试断言                                  │
│  直接测试类方法行为，验证逻辑正确性                        │
│  示例: EntryVoterTest::testVoteReturnsGrantedFor...()   │
├─────────────────────────────────────────────────────────┤
│  优先级 3: 生产代码实现                                  │
│  实体定义、配置文件、控制器、Voter 等                      │
│  示例: src/Entity/Api/Client.php                         │
├─────────────────────────────────────────────────────────┤
│  优先级 4: 外部 Bundle 公开文档                          │
│  FOSOAuthServerBundle、SensioFrameworkExtraBundle 文档   │
├─────────────────────────────────────────────────────────┤
│  优先级 5: 行业惯例/经验推断                             │
│  最不可信，仅在无其他证据时作为参考                        │
└─────────────────────────────────────────────────────────┘
```

### 冲突处理规则

1. 当测试断言与生产代码一致时：**高可信度结论**
2. 当测试断言与外部 Bundle 文档一致时：**中等可信度结论**
3. 当仅有外部 Bundle 文档或惯例推断时：**明确标注「无法在仓内证实」**
4. 当不同优先级证据冲突时：**以高优先级证据为准**，并标注冲突

---

## 一、/oauth/v2/token 令牌颁发链路分析

### 1.1 仓内可直接证实的事实

| 序号 | 事实描述 | 代码证据 |
|------|----------|----------|
| 1.1.1 | 路由 `fos_oauth_server_token` 引用 FOSOAuthServerBundle 的 token.xml 路由资源 | `app/config/routing.yml:26-27` |
| 1.1.2 | `/oauth/v2/token` 路径被配置为无需安全认证（`security: false`） | `app/config/security.yml:25-27` |
| 1.1.3 | FOSOAuthServerBundle 配置使用自定义实体类：<br>- Client: `Wallabag\Entity\Api\Client`<br>- AccessToken: `Wallabag\Entity\Api\AccessToken`<br>- RefreshToken: `Wallabag\Entity\Api\RefreshToken`<br>- AuthCode: `Wallabag\Entity\Api\AuthCode` | `app/config/config.yml:203-208` |
| 1.1.4 | 令牌生命周期通过环境变量配置：<br>- `WALLABAG_OAUTH_REFRESH_TOKEN_LIFETIME`<br>- `WALLABAG_OAUTH_ACCESS_TOKEN_LIFETIME` | `app/config/config.yml:212-213` |
| 1.1.5 | 用户提供器使用 `fos_user.user_provider.username_email` | `app/config/config.yml:210` |
| 1.1.6 | Client 实体继承自 `FOS\OAuthServerBundle\Entity\Client` | `src/Entity/Api/Client.php:6,16` |
| 1.1.7 | AccessToken 实体继承自 `FOS\OAuthServerBundle\Entity\AccessToken` | `src/Entity/Api/AccessToken.php:6,11` |
| 1.1.8 | RefreshToken 实体继承自 `FOS\OAuthServerBundle\Entity\RefreshToken` | `src/Entity/Api/RefreshToken.php:6,11` |
| 1.1.9 | Client 创建时被设置支持 4 种授权类型：<br>`token`, `authorization_code`, `password`, `refresh_token` | `src/Controller/Api/DeveloperController.php:47` |
| 1.1.10 | Client 与 AccessToken、RefreshToken 配置了 ORM 级联删除（`cascade: ['remove']`） | `src/Entity/Api/Client.php:36-40` |
| 1.1.11 | AccessToken 和 RefreshToken 的 user 外键配置了数据库级联删除（`onDelete: 'CASCADE'`） | `src/Entity/Api/AccessToken.php:22`<br>`src/Entity/Api/RefreshToken.php:22` |
| 1.1.12 | Client 实体的 client_id 格式为 `{id}_{randomId}` | `src/Entity/Api/Client.php:103-109` |
| 1.1.13 | 删除 Client 时，EntityManager 执行 remove 操作 | `src/Controller/Api/DeveloperController.php:84` |
| 1.1.14 | 删除 Client 前会验证用户是否为 Client 所有者 | `src/Controller/Api/DeveloperController.php:80-82` |
| 1.1.15 | 删除 Client 操作需要 CSRF 令牌验证 | `src/Controller/Api/DeveloperController.php:76-78` |
| 1.1.16 | API 层防火墙配置 `fos_oauth: true` 和 `stateless: true` | `app/config/security.yml:29-34` |
| 1.1.17 | Web 层防火墙配置 `form_login` 和 Session 认证 | `app/config/security.yml:40-61` |
| 1.1.18 | Web 层支持双因素认证（2FA） | `app/config/security.yml:57-60` |
| 1.1.19 | **功能测试证实**：POST `/oauth/v2/token` 使用密码模式可成功获取令牌 | `tests/functional/Controller/Api/DeveloperControllerTest.php:39-59` |
| 1.1.20 | **功能测试证实**：令牌响应包含 `access_token`、`expires_in`、`token_type`、`refresh_token` 四个字段 | `tests/functional/Controller/Api/DeveloperControllerTest.php:55-58` |
| 1.1.21 | **功能测试证实**：错误的 client_id 会导致 400 响应 | `tests/functional/Controller/Api/DeveloperControllerTest.php:61-73` |
| 1.1.22 | **功能测试证实**：使用 Client::getPublicId() 和 Client::getSecret() 可通过 OAuth2 验证 | `tests/functional/Controller/Api/DeveloperControllerTest.php:46-47` |

---

### 1.2 测试证据的证明力边界

#### 1.2.1 功能测试能证明的

| 测试方法 | 能证明 | 不能证明 | 证据位置 |
|----------|--------|----------|----------|
| `testCreateToken()` | 1. `/oauth/v2/token` 端点可访问<br>2. 密码模式 grant_type 有效<br>3. 响应包含 access_token、expires_in、token_type、refresh_token<br>4. HTTP 状态码 200<br>5. client_id + client_secret 验证机制存在<br>6. username + password 验证机制存在 | 1. 令牌生成算法<br>2. 令牌是否持久化到数据库<br>3. 内部验证步骤的顺序和细节<br>4. expires_in 的具体值来源 | `tests/functional/Controller/Api/DeveloperControllerTest.php:39-59` |
| `testCreateTokenWithBadClientId()` | 1. 错误凭证时返回 400 状态码<br>2. 客户端凭证验证机制存在 | 1. 错误码的具体类型<br>2. 验证失败的具体原因 | `tests/functional/Controller/Api/DeveloperControllerTest.php:61-73` |
| `testCreateClient()` | 1. 客户端可成功创建<br>2. 创建后数量增加<br>3. 名称可回显 | 1. allowed_grant_types 是否生效<br>2. 级联删除是否工作 | `tests/functional/Controller/Api/DeveloperControllerTest.php:12-37` |
| `testRemoveClient()` | 1. 非所有者无法删除客户端<br>2. 所有者可删除客户端<br>3. 删除后数据库中 Client 记录消失 | 1. 级联删除是否删除了 AccessToken/RefreshToken<br>2. 删除后旧令牌是否立即失效 | `tests/functional/Controller/Api/DeveloperControllerTest.php:96-124` |

#### 1.2.2 单元测试能证明的

| 测试类 | 能证明 | 不能证明 | 证据位置 |
|--------|--------|----------|----------|
| `EntryVoterTest` | 1. EntryVoter 支持 14 种权限属性<br>2. 非 Entry 实例返回 ACCESS_ABSTAIN<br>3. 非所有者返回 ACCESS_DENIED<br>4. 所有者返回 ACCESS_GRANTED<br>5. 所有 14 种权限的判定逻辑一致 | 1. Voter 在实际 HTTP 请求中的调用时机<br>2. Voter 链的决策策略<br>3. 与其他 Voter 的交互 | `tests/unit/Security/Voter/EntryVoterTest.php` |
| `MainVoterTest` | 1. MainVoter 支持 12 种权限属性<br>2. 非 null subject 返回 ACCESS_ABSTAIN<br>3. 无 ROLE_USER 返回 ACCESS_DENIED<br>4. 有 ROLE_USER 返回 ACCESS_GRANTED | 1. Voter 在实际 HTTP 请求中的调用时机<br>2. Voter 链的决策策略 | `tests/unit/Security/Voter/MainVoterTest.php` |

---

### 1.3 仅能推断的环节（外部 Bundle 实现，无法在仓内证实）

> **注**：功能测试已证实端点可用且返回正确格式，但以下内部实现细节无法从仓内代码直接观察。

| 序号 | 推断描述 | 依据 | 可证实性 |
|------|----------|------|----------|
| 1.3.1 | FOSOAuthServerBundle 的 token.xml 中定义了 `/oauth/v2/token` 路由，指向 `fos_oauth_server.controller.token:tokenAction` | 路由配置引用外部 Bundle 资源 | **无法在仓内证实** |
| 1.3.2 | TokenController::tokenAction 是令牌颁发的入口方法 | Bundle 命名惯例 + 路由配置 | **无法在仓内证实** |
| 1.3.3 | 令牌颁发过程包含：<br>1. 验证 grant_type<br>2. 验证客户端凭证（client_id + client_secret）<br>3. 验证用户凭证（username + password，密码模式）<br>4. 生成 AccessToken 和 RefreshToken<br>5. 持久化到数据库<br>6. 返回 JSON 响应 | 功能测试证实端到端可用 + OAuth2 协议规范 | **无法在仓内证实** |
| 1.3.4 | AccessToken 生成使用 `bin2hex(random_bytes(40))` 或类似随机算法 | Symfony Security 组件惯例 | **无法在仓内证实** |
| 1.3.5 | 令牌验证时从数据库查询 token 并验证有效性 | FOSOAuthServerBundle 标准行为 | **无法在仓内证实** |
| 1.3.6 | Refresh Token 流程会撤销旧的 Access Token 并生成新的令牌对 | OAuth2 规范常见实现 | **无法在仓内证实** |
| 1.3.7 | `fos_oauth` 防火墙监听器会从 `Authorization: Bearer` header 提取 token | FOSOAuthServerBundle 标准行为 | **无法在仓内证实** |

---

### 1.4 令牌颁发调用链总结（修正版）

```
┌─────────────────────────────────────────────────────────┐
│  【可证实 + 功能测试验证】                                │
└─────────────────────────────────────────────────────────┘
POST /oauth/v2/token
    ↓
【可证实】路由匹配: app/config/routing.yml:26-27
    ↓
【可证实】防火墙: app/config/security.yml:25-27 → security: false
    ↓
【可证实】请求参数: grant_type=password, client_id, client_secret, username, password
    ↓
【可证实·功能测试】返回 200 状态码
    ↓
【可证实·功能测试】响应包含 access_token, expires_in, token_type, refresh_token
    ↓
═══════════════════════════════════════════════════════════════
┌─────────────────────────────────────────────────────────┐
│  【无法在仓内证实】外部 Bundle 内部实现                    │
└─────────────────────────────────────────────────────────┘
    ↓
TokenController::tokenAction()
    ↓
OAuth2 Server 验证 grant_type
    ↓
验证客户端凭证 (client_id + client_secret)
    ↓
验证用户凭证 (username + password，密码模式)
    ↓
生成 AccessToken + RefreshToken
    ↓
EntityManager::flush() 持久化
    ↓
返回 JSON 响应
```

---

### 1.5 可证实边界修正总结

| 内容 | 原状态 | 新状态 | 原因 |
|------|--------|--------|------|
| 端点可用性 | 仅推断 | **可证实** | 功能测试 `testCreateToken()` 直接调用并验证 200 响应 |
| 响应格式 | 仅推断 | **可证实** | 功能测试断言 access_token、expires_in、token_type、refresh_token 存在 |
| 密码模式支持 | 仅推断 | **可证实** | 功能测试使用 `grant_type=password` 成功获取令牌 |
| 客户端凭证验证 | 仅推断 | **可证实** | 功能测试 `testCreateTokenWithBadClientId()` 验证错误凭证返回 400 |
| 令牌持久化 | 仅推断 | **仍无法证实** | 功能测试未查询数据库验证令牌是否存储 |
| 令牌生成算法 | 仅推断 | **仍无法证实** | 仓内无 token 生成代码 |
| 内部验证顺序 | 仅推断 | **仍无法证实** | 外部 Bundle 实现，无法观察 |

---

## 二、权限判定触发机制分析

### 2.1 仓内可直接证实的事实

| 序号 | 事实描述 | 代码证据 |
|------|----------|----------|
| 2.1.1 | API 层控制器导入 `Sensio\Bundle\FrameworkExtraBundle\Configuration\IsGranted` | `src/Controller/Api/EntryRestController.php:11` |
| 2.1.2 | Web 层控制器导入 `Sensio\Bundle\FrameworkExtraBundle\Configuration\IsGranted` | `src/Controller/EntryController.php:11` |
| 2.1.3 | API 层 Entry 列表方法使用 `#[IsGranted('LIST_ENTRIES')]` | `src/Controller/Api/EntryRestController.php:91` |
| 2.1.4 | API 层单 Entry 查看方法使用 `#[IsGranted('VIEW', subject: 'entry')]` | `src/Controller/Api/EntryRestController.php:406` |
| 2.1.5 | API 层单 Entry 删除方法使用 `#[IsGranted('DELETE', subject: 'entry')]` | `src/Controller/Api/EntryRestController.php:1107` |
| 2.1.6 | Web 层 Entry 列表方法使用 `#[IsGranted('LIST_ENTRIES')]` | `src/Controller/EntryController.php:145` |
| 2.1.7 | Web 层单 Entry 查看方法使用 `#[IsGranted('VIEW', subject: 'entry')]` | `src/Controller/EntryController.php:389` |
| 2.1.8 | Web 层单 Entry 删除方法使用 `#[IsGranted('DELETE', subject: 'entry')]` | `src/Controller/EntryController.php:499` |
| 2.1.9 | Web 层 POST 操作在控制器方法内执行 CSRF 验证 | `src/Controller/EntryController.php:500-502` |
| 2.1.10 | MainVoter 定义集合级权限常量：`LIST_ENTRIES`, `CREATE_ENTRIES` 等 | `src/Security/Voter/MainVoter.php:11-22` |
| 2.1.11 | EntryVoter 定义单条目权限常量：`VIEW`, `EDIT`, `DELETE` 等 | `src/Security/Voter/EntryVoter.php:12-25` |
| 2.1.12 | MainVoter 的 `supports()` 要求 subject 为 `null` | `src/Security/Voter/MainVoter.php:29-33` |
| 2.1.13 | EntryVoter 的 `supports()` 要求 subject 为 `Entry` 实例 | `src/Security/Voter/EntryVoter.php:27-31` |
| 2.1.14 | MainVoter 的 `voteOnAttribute()` 检查 `ROLE_USER` | `src/Security/Voter/MainVoter.php:42-48` |
| 2.1.15 | EntryVoter 的 `voteOnAttribute()` 检查 `$user === $subject->getUser()` | `src/Security/Voter/EntryVoter.php:40-54` |
| 2.1.16 | Entry 实体的 `getUser()` 方法返回关联的 User 对象 | `src/Entity/Entry.php:457-460` |
| 2.1.17 | EntryRepository::findEntries() 按 userId 过滤数据 | `src/Repository/EntryRepository.php:282,290` |
| 2.1.18 | EntryRepository 多个查询方法都包含 `e.user = :userId` 条件 | `src/Repository/EntryRepository.php:520,540,551,565` |
| 2.1.19 | **单元测试证实**：EntryVoter 对非 Entry 实例返回 ACCESS_ABSTAIN | `tests/unit/Security/Voter/EntryVoterTest.php:28-31` |
| 2.1.20 | **单元测试证实**：EntryVoter 对无效属性返回 ACCESS_ABSTAIN | `tests/unit/Security/Voter/EntryVoterTest.php:33-36` |
| 2.1.21 | **单元测试证实**：EntryVoter 对非条目所有者返回 ACCESS_DENIED（14 种权限全覆盖） | `tests/unit/Security/Voter/EntryVoterTest.php:38-224` |
| 2.1.22 | **单元测试证实**：EntryVoter 对条目所有者返回 ACCESS_GRANTED（14 种权限全覆盖） | `tests/unit/Security/Voter/EntryVoterTest.php:38-224` |
| 2.1.23 | **单元测试证实**：MainVoter 对非 null subject 返回 ACCESS_ABSTAIN | `tests/unit/Security/Voter/MainVoterTest.php:33-36` |
| 2.1.24 | **单元测试证实**：MainVoter 对无效属性返回 ACCESS_ABSTAIN | `tests/unit/Security/Voter/MainVoterTest.php:28-31` |
| 2.1.25 | **单元测试证实**：MainVoter 对无 ROLE_USER 用户返回 ACCESS_DENIED | `tests/unit/Security/Voter/MainVoterTest.php:38-204` |
| 2.1.26 | **单元测试证实**：MainVoter 对有 ROLE_USER 用户返回 ACCESS_GRANTED | `tests/unit/Security/Voter/MainVoterTest.php:38-204` |

---

### 2.2 权限判定测试证据的证明力边界

| 测试类 | 能证明 | 不能证明 | 证据位置 |
|--------|--------|----------|----------|
| `EntryVoterTest` | 1. `supports()` 方法逻辑：非 Entry 或无效属性 → ABSTAIN<br>2. `voteOnAttribute()` 方法逻辑：非所有者 → DENIED<br>3. `voteOnAttribute()` 方法逻辑：所有者 → GRANTED<br>4. 14 种权限属性全部按相同逻辑判定 | 1. Voter 在实际 HTTP 请求中的调用时机<br>2. Voter 链的决策策略（affirmative/consensus/unanimous）<br>3. Voter 的调用顺序<br>4. 与其他 Voter 的交互 | `tests/unit/Security/Voter/EntryVoterTest.php` |
| `MainVoterTest` | 1. `supports()` 方法逻辑：非 null 或无效属性 → ABSTAIN<br>2. `voteOnAttribute()` 方法逻辑：无 ROLE_USER → DENIED<br>3. `voteOnAttribute()` 方法逻辑：有 ROLE_USER → GRANTED<br>4. 12 种权限属性全部按相同逻辑判定 | 1. Voter 在实际 HTTP 请求中的调用时机<br>2. Voter 链的决策策略<br>3. Voter 的调用顺序 | `tests/unit/Security/Voter/MainVoterTest.php` |
| `DeveloperControllerTest` | 1. `#[IsGranted]` 注解在实际 HTTP 请求中生效<br>2. 无权限用户访问受保护端点会被拒绝 | 1. `#[IsGranted]` 注解的具体触发事件<br>2. 注解解析的内部实现 | `tests/functional/Controller/Api/DeveloperControllerTest.php:107-109` |

---

### 2.3 仅能推断的环节（外部 Bundle 实现，无法在仓内证实）

| 序号 | 推断描述 | 依据 | 可证实性 |
|------|----------|------|----------|
| 2.3.1 | `#[IsGranted]` 注解由 `SensioFrameworkExtraBundle` 的事件监听器处理 | Bundle 公开文档 + 行业惯例 | **无法在仓内证实** |
| 2.3.2 | 事件监听器监听 `kernel.controller_arguments` 事件 | Symfony 事件系统惯例 | **无法在仓内证实** |
| 2.3.3 | 监听器在控制器方法执行前解析注解并调用 `AuthorizationChecker::isGranted()` | Bundle 公开文档 + 功能测试验证注解生效 | **无法在仓内证实** |
| 2.3.4 | `subject: 'entry'` 会从控制器参数中解析出 Entry 对象 | Bundle 公开文档 | **无法在仓内证实** |
| 2.3.5 | Symfony 的 `AccessDecisionManager` 遍历所有注册的 Voter 进行决策 | Symfony Security 核心机制 | **无法在仓内证实** |
| 2.3.6 | Voter 的 `supports()` 和 `voteOnAttribute()` 按顺序被调用 | Symfony Security 核心机制 + 单元测试验证方法逻辑 | **无法在仓内证实** |
| 2.3.7 | 权限不通过时抛出 `AccessDeniedException` | Symfony Security 标准行为 + 功能测试验证 403 响应 | **无法在仓内证实** |
| 2.3.8 | `ParamConverter` 自动将路由参数 `{entry}` 转换为 Entry 实体对象 | SensioFrameworkExtraBundle 标准行为 | **无法在仓内证实** |

---

## 三、Web 层与 API 层权限判定关键节点与执行顺序

### 3.1 API 层完整执行路径（可证实节点）

```
请求: GET /api/entries/123.json
Header: Authorization: Bearer {token}
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】路由匹配                                      │
│ 证据: src/Controller/Api/EntryRestController.php:404-405│
│ #[Route(path: '/api/entries/{entry}.{_format}', ...)]   │
│ #[IsGranted('VIEW', subject: 'entry')]                  │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】防火墙匹配: api                               │
│ 证据: app/config/security.yml:29-34                    │
│ pattern: /api/.*                                        │
│ fos_oauth: true                                         │
│ stateless: true                                         │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】OAuth2 认证（FOSOAuthServerBundle 实现）        │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】TokenStorage 存储认证 Token                     │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】ParamConverter 加载 Entry 实体                  │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】kernel.controller_arguments 事件触发            │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】IsGrantedAttributeListener 执行                 │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】EntryVoter::supports()                        │
│ 证据: src/Security/Voter/EntryVoter.php:27-38          │
│ 检查 $subject instanceof Entry                          │
│ 检查 $attribute 在权限常量列表中                        │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】EntryVoter::voteOnAttribute()                 │
│ 证据: src/Security/Voter/EntryVoter.php:40-54          │
│ 核心逻辑: $user === $subject->getUser()                 │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】控制器方法执行                                │
│ 证据: src/Controller/Api/EntryRestController.php:406   │
│ 无 CSRF 检查                                            │
│ 返回 JSON 响应                                          │
└─────────────────────────────────────────────────────────┘
```

---

### 3.2 Web 层完整执行路径（可证实节点）

```
请求: POST /delete/123
Cookie: PHPSESSID={session_id}
Body: token={csrf_token}
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】路由匹配                                      │
│ 证据: src/Controller/EntryController.php:497-499        │
│ #[Route(path: '/delete/{id}', ...)]                     │
│ #[IsGranted('DELETE', subject: 'entry')]                │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】防火墙匹配: main                              │
│ 证据: app/config/security.yml:40-61                    │
│ pattern: ^/                                             │
│ form_login: 启用                                        │
│ two_factor: 启用（可选）                                │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】Session 认证（Symfony Security 实现）           │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】TokenStorage 存储认证 Token                     │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】2FA 检查（如果用户启用）                        │
│ 证据: app/config/security.yml:57-60                    │
│ 具体实现无法在仓内证实                                  │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】ParamConverter 加载 Entry 实体                  │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】kernel.controller_arguments 事件触发            │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【推断】IsGrantedAttributeListener 执行                 │
│ 无法在仓内证实                                          │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】EntryVoter::supports()                        │
│ 证据: src/Security/Voter/EntryVoter.php:27-38          │
│ 与 API 层完全相同                                       │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】EntryVoter::voteOnAttribute()                 │
│ 证据: src/Security/Voter/EntryVoter.php:40-54          │
│ 核心逻辑: $user === $subject->getUser()                 │
│ 与 API 层完全相同                                       │
└───────────────────────────┬─────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────┐
│ 【可证实】控制器方法执行                                │
│ 证据: src/Controller/EntryController.php:499-531       │
│ Web 层特有: CSRF 令牌验证 (行 500-502)                  │
│ Web 层特有: 重定向响应                                  │
└─────────────────────────────────────────────────────────┘
```

---

### 3.3 两层关键节点对比表

| 执行阶段 | API 层（可证实性） | Web 层（可证实性） | 异同 |
|----------|-------------------|-------------------|------|
| **路由匹配** | 可证实 `#[Route]` 注解 | 可证实 `#[Route]` 注解 | 相同机制 |
| **防火墙匹配** | 可证实 `api` 防火墙配置 | 可证实 `main` 防火墙配置 | 配置不同，机制相同 |
| **认证方式** | 可证实配置 `fos_oauth: true`<br>**具体实现无法证实** | 可证实配置 `form_login`<br>**具体实现无法证实** | 认证方式不同 |
| **2FA 支持** | 无配置 | 可证实配置存在<br>**具体实现无法证实** | Web 层独有 |
| **Token 存储** | 无法证实 | 无法证实 | 机制相同（Symfony 内部） |
| **ParamConverter** | 无法证实 | 无法证实 | 机制相同 |
| **事件触发** | 无法证实 | 无法证实 | 机制相同 |
| **IsGranted 解析** | 可证实注解存在<br>**具体实现无法证实** | 可证实注解存在<br>**具体实现无法证实** | 机制相同 |
| **Voter::supports()** | **完全可证实** | **完全可证实** | 代码完全相同 |
| **Voter::voteOnAttribute()** | **完全可证实** | **完全可证实** | 代码完全相同 |
| **CSRF 保护** | 无（可证实） | **可证实**存在于控制器内 | Web 层独有 |
| **响应方式** | 可证实返回 JSON | 可证实返回重定向/HTML | 不同 |
| **数据过滤（Repository）** | **可证实**按 userId 过滤 | **可证实**按 userId 过滤 | 代码完全相同 |

---

### 3.4 权限判定复用的关键证据

**证据 1：同一套 Voter 被两层共享**
- Voter 类无任何条件判断区分 API 层和 Web 层
- 代码位置：`src/Security/Voter/MainVoter.php` 和 `src/Security/Voter/EntryVoter.php`

**证据 2：同一套权限常量定义**
```php
// src/Security/Voter/EntryVoter.php:12-25
public const VIEW = 'VIEW';
public const EDIT = 'EDIT';
public const DELETE = 'DELETE';
// ... 14 种权限常量
```

**证据 3：同一套判定逻辑**
```php
// src/Security/Voter/EntryVoter.php:50-51
self::VIEW, self::EDIT, self::DELETE, ... => $user === $subject->getUser(),
```

**证据 4：API 层使用示例**
```php
// src/Controller/Api/EntryRestController.php:406
#[IsGranted('VIEW', subject: 'entry')]
public function getEntryAction(Entry $entry)
```

**证据 5：Web 层使用示例**
```php
// src/Controller/EntryController.php:389
#[IsGranted('VIEW', subject: 'entry')]
public function viewAction(Entry $entry)
```

---

## 四、可核对结论汇总

### 4.1 100% 可证实的结论（含测试证据）

#### 4.1.1 OAuth2 令牌链路
1. **端点可用性**：`POST /oauth/v2/token` 可访问，密码模式 grant_type 有效，返回 200 状态码。
   - 证据：`tests/functional/Controller/Api/DeveloperControllerTest.php:39-59`
   
2. **响应格式**：令牌响应包含 `access_token`、`expires_in`、`token_type`、`refresh_token` 四个字段。
   - 证据：`tests/functional/Controller/Api/DeveloperControllerTest.php:55-58`
   
3. **客户端凭证验证**：错误的 client_id 会导致 400 响应。
   - 证据：`tests/functional/Controller/Api/DeveloperControllerTest.php:61-73`

4. **令牌级联删除配置**：
   - ORM 层面：Client 配置了 `cascade: ['remove']` 级联删除 AccessToken 和 RefreshToken
   - 数据库层面：AccessToken 和 RefreshToken 配置了 `onDelete: 'CASCADE'`
   - 证据：`src/Entity/Api/Client.php:36-40`、`src/Entity/Api/AccessToken.php:22`

#### 4.1.2 权限判定链路
5. **Voter 系统完全复用**：MainVoter 和 EntryVoter 的代码被 API 层和 Web 层完全共享，无任何分支逻辑区分调用来源。
   - 证据：`src/Security/Voter/MainVoter.php`、`src/Security/Voter/EntryVoter.php`

6. **EntryVoter 逻辑正确性**（单元测试全覆盖）：
   - 非 Entry 实例 → ACCESS_ABSTAIN
   - 无效属性 → ACCESS_ABSTAIN
   - 非条目所有者 → ACCESS_DENIED
   - 条目所有者 → ACCESS_GRANTED
   - 14 种权限全部按相同逻辑判定
   - 证据：`tests/unit/Security/Voter/EntryVoterTest.php`（26 个测试用例）

7. **MainVoter 逻辑正确性**（单元测试全覆盖）：
   - 非 null subject → ACCESS_ABSTAIN
   - 无效属性 → ACCESS_ABSTAIN
   - 无 ROLE_USER → ACCESS_DENIED
   - 有 ROLE_USER → ACCESS_GRANTED
   - 12 种权限全部按相同逻辑判定
   - 证据：`tests/unit/Security/Voter/MainVoterTest.php`（22 个测试用例）

8. **权限判定核心逻辑**：单条目权限判定的核心是 `$user === $entry->getUser()`，即用户必须是条目所有者。
   - 证据：`src/Security/Voter/EntryVoter.php:50-51`

9. **集合级权限判定**：仅检查用户是否拥有 `ROLE_USER` 角色。
   - 证据：`src/Security/Voter/MainVoter.php:45`

10. **双重数据安全**：
    - Voter 层：权限检查
    - Repository 层：所有查询方法自动按 userId 过滤（`e.user = :userId`）
    - 证据：`src/Repository/EntryRepository.php:290,520,540,551,565`

11. **CSRF 保护差异**：Web 层 POST/PUT/DELETE 操作需要 CSRF 令牌验证，API 层不需要（无状态设计）。
    - 证据：`src/Controller/EntryController.php:500-502`

12. **防火墙配置差异**：
    - API 层：`fos_oauth: true`, `stateless: true`
    - Web 层：`form_login`, Session 认证, 支持 2FA
    - 证据：`app/config/security.yml:29-61`

---

### 4.2 无法在仓内证实的结论（需依赖外部 Bundle 文档）

> **注**：以下内容虽有功能测试验证端到端行为可用，但内部实现细节无法从仓内代码直接观察。

1. `#[IsGranted]` 注解的具体触发时机和实现细节。
2. FOSOAuthServerBundle 令牌颁发的完整内部流程（验证顺序、令牌生成算法等）。
3. OAuth2 认证监听器的具体实现（如何从 Authorization header 提取和验证 token）。
4. ParamConverter 的参数转换机制（如何将路由参数转换为实体对象）。
5. Symfony AccessDecisionManager 的具体决策策略（affirmative/consensus/unanimous）。

---

### 4.3 边界修正总结

| 内容 | 前版状态 | 现版状态 | 修正依据 |
|------|----------|----------|----------|
| `/oauth/v2/token` 端点可用性 | 仅推断 | **可证实** | 功能测试直接调用并验证 200 响应 |
| 令牌响应格式 | 仅推断 | **可证实** | 功能测试断言 access_token 等 4 个字段存在 |
| 密码模式支持 | 仅推断 | **可证实** | 功能测试使用 `grant_type=password` 成功获取令牌 |
| 客户端凭证验证 | 仅推断 | **可证实** | 功能测试验证错误凭证返回 400 |
| EntryVoter 方法逻辑 | 仅代码静态分析 | **可证实** | 单元测试 26 个用例全覆盖 |
| MainVoter 方法逻辑 | 仅代码静态分析 | **可证实** | 单元测试 22 个用例全覆盖 |
| `#[IsGranted]` 注解生效 | 仅代码静态分析 | **可证实** | 功能测试验证无权限用户被拒绝 |
| 令牌生成算法 | 仅推断 | **仍无法证实** | 仓内无 token 生成代码 |
| 令牌持久化过程 | 仅推断 | **仍无法证实** | 功能测试未查询数据库验证 |
| Voter 调用时机和顺序 | 仅推断 | **仍无法证实** | 外部 Bundle 实现 |

---

### 4.4 关键代码索引

| 功能模块 | 核心文件 |
|----------|----------|
| 客户端管理 | `src/Controller/Api/DeveloperController.php` |
| Client 实体 | `src/Entity/Api/Client.php` |
| AccessToken 实体 | `src/Entity/Api/AccessToken.php` |
| RefreshToken 实体 | `src/Entity/Api/RefreshToken.php` |
| 集合级权限 Voter | `src/Security/Voter/MainVoter.php` |
| 单条目权限 Voter | `src/Security/Voter/EntryVoter.php` |
| API 层 Entry 控制器 | `src/Controller/Api/EntryRestController.php` |
| Web 层 Entry 控制器 | `src/Controller/EntryController.php` |
| Entry Repository | `src/Repository/EntryRepository.php` |
| 安全配置 | `app/config/security.yml` |
| OAuth2 配置 | `app/config/config.yml:203-213` |
| 路由配置 | `app/config/routing.yml:26-27` |
| **令牌功能测试** | `tests/functional/Controller/Api/DeveloperControllerTest.php` |
| **EntryVoter 单元测试** | `tests/unit/Security/Voter/EntryVoterTest.php` |
| **MainVoter 单元测试** | `tests/unit/Security/Voter/MainVoterTest.php` |
