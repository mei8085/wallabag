# Wallabag 鉴权机制分析报告

## 一、概述

Wallabag 的鉴权机制基于 **OAuth2 协议** 实现，使用 `FOSOAuthServerBundle` 作为核心组件。系统支持第三方客户端通过标准的 OAuth2 流程获取访问令牌，进而对条目（Entry）资源进行读写操作。

## 二、核心实体结构

### 2.1 Client（客户端）
**文件位置**：`src/Entity/Api/Client.php`

```php
#[ORM\Table('oauth2_clients')]
class Client extends BaseClient
{
    protected $id;
    protected $name;           // 客户端名称
    protected $secret;         // 客户端密钥
    protected $user;           // 所属用户
    
    #[ORM\OneToMany(targetEntity: RefreshToken::class, mappedBy: 'client', cascade: ['remove'])]
    protected $refreshTokens;  // 级联删除关联的刷新令牌
    
    #[ORM\OneToMany(targetEntity: AccessToken::class, mappedBy: 'client', cascade: ['remove'])]
    protected $accessTokens;   // 级联删除关联的访问令牌
}
```

**关键特性**：
- 每个客户端属于一个用户（`$user`）
- 客户端 ID 格式：`{id}_{randomId}`（通过 `getClientId()` 方法生成）
- 支持的授权类型：`token`, `authorization_code`, `password`, `refresh_token`

### 2.2 AccessToken（访问令牌）
**文件位置**：`src/Entity/Api/AccessToken.php`

```php
#[ORM\Table('oauth2_access_tokens')]
class AccessToken extends BaseAccessToken
{
    #[ORM\ManyToOne(targetEntity: Client::class)]
    protected $client;
    
    #[ORM\ManyToOne(targetEntity: User::class)]
    #[ORM\JoinColumn(onDelete: 'CASCADE')]
    protected $user;
}
```

### 2.3 RefreshToken（刷新令牌）
**文件位置**：`src/Entity/Api/RefreshToken.php`

与 AccessToken 结构类似，关联 Client 和 User，用户删除时数据库级联删除。

### 2.4 AuthCode（授权码）
**文件位置**：`src/Entity/Api/AuthCode.php`

用于 Authorization Code 授权流程的中间凭证。

---

## 三、客户端注册流程

### 3.1 注册入口
**文件位置**：`src/Controller/Api/DeveloperController.php:39-66`

```php
#[Route(path: '/developer/client/create', name: 'developer_create_client')]
public function createClientAction(Request $request, ...)
{
    $client = new Client($this->getUser());
    $clientForm = $this->createForm(ClientType::class, $client);
    $clientForm->handleRequest($request);

    if ($clientForm->isSubmitted() && $clientForm->isValid()) {
        $client->setAllowedGrantTypes([
            'token', 
            'authorization_code', 
            'password', 
            'refresh_token'
        ]);
        $entityManager->persist($client);
        $entityManager->flush();
        
        return $this->render('Developer/client_parameters.html.twig', [
            'client_id' => $client->getPublicId(),
            'client_secret' => $client->getSecret(),
            'client_name' => $client->getName(),
        ]);
    }
}
```

### 3.2 注册表单
**文件位置**：`src/Form/Type/Api/ClientType.php`

用户仅需提供：
- `name`: 客户端名称
- `redirect_uris`: 重定向 URI（可选）

系统自动生成：
- `randomId`: 随机字符串
- `secret`: 客户端密钥

### 3.3 注册完整流程

```
用户访问 /developer
    ↓
点击 "Create a new client"
    ↓
填写客户端名称和重定向 URI
    ↓
提交表单 → DeveloperController::createClientAction
    ↓
创建 Client 实体，关联当前用户
    ↓
设置支持的授权类型（4种）
    ↓
持久化到数据库
    ↓
返回 client_id 和 client_secret 给用户
```

---

## 四、授权令牌颁发流程

### 4.1 安全配置
**文件位置**：`app/config/security.yml:25-34`

```yaml
firewalls:
    oauth_token:
        pattern: ^/oauth/v2/token
        security: false    # 令牌端点公开访问

    api:
        pattern: /api/.*
        fos_oauth: true     # 启用 OAuth2 认证
        stateless: true     # 无状态
        anonymous: true
        provider: fos_userbundle
```

### 4.2 令牌端点配置
**文件位置**：`app/config/routing.yml:26-27`

```yaml
fos_oauth_server_token:
    resource: "@FOSOAuthServerBundle/Resources/config/routing/token.xml"
```

令牌端点路径：`/oauth/v2/token`

### 4.3 OAuth2 配置
**文件位置**：`app/config/config.yml:203-213`

```yaml
fos_oauth_server:
    db_driver: orm
    client_class: Wallabag\Entity\Api\Client
    access_token_class: Wallabag\Entity\Api\AccessToken
    refresh_token_class: Wallabag\Entity\Api\RefreshToken
    auth_code_class: Wallabag\Entity\Api\AuthCode
    service:
        user_provider: fos_user.user_provider.username_email
        options:
            refresh_token_lifetime: "%env(int:WALLABAG_OAUTH_REFRESH_TOKEN_LIFETIME)%"
            access_token_lifetime: "%env(int:WALLABAG_OAUTH_ACCESS_TOKEN_LIFETIME)%"
```

### 4.4 支持的授权类型

1. **Authorization Code（授权码模式）**
   - 适用于第三方 Web 应用
   - 流程：用户授权 → 获取 code → 用 code 换 token

2. **Implicit（简化模式）**
   - 适用于浏览器端应用
   - 直接在重定向中返回 access token

3. **Resource Owner Password Credentials（密码模式）**
   - 适用于受信任的客户端
   - 用户直接提供用户名密码换取 token

4. **Refresh Token（刷新令牌）**
   - access token 过期后，使用 refresh token 获取新的 access token

---

## 五、条目访问权限控制

### 5.1 权限 Voter 架构

Wallabag 使用 Symfony 的 Voter 系统实现细粒度权限控制。

#### 5.1.1 MainVoter（集合级权限）
**文件位置**：`src/Security/Voter/MainVoter.php`

```php
class MainVoter extends Voter
{
    public const LIST_ENTRIES = 'LIST_ENTRIES';
    public const CREATE_ENTRIES = 'CREATE_ENTRIES';
    public const EDIT_ENTRIES = 'EDIT_ENTRIES';
    public const DELETE_ENTRIES = 'DELETE_ENTRIES';
    // ... 其他权限常量

    protected function voteOnAttribute(string $attribute, $subject, TokenInterface $token): bool
    {
        return match ($attribute) {
            self::LIST_ENTRIES, self::CREATE_ENTRIES, ... => 
                $this->security->isGranted('ROLE_USER'),
            default => false,
        };
    }
}
```

**集合级权限**：仅检查用户是否有 `ROLE_USER` 角色。

#### 5.1.2 EntryVoter（单条目权限）
**文件位置**：`src/Security/Voter/EntryVoter.php`

```php
class EntryVoter extends Voter
{
    public const VIEW = 'VIEW';
    public const EDIT = 'EDIT';
    public const DELETE = 'DELETE';
    public const STAR = 'STAR';
    public const ARCHIVE = 'ARCHIVE';
    public const SHARE = 'SHARE';
    // ... 共14种操作权限

    protected function voteOnAttribute(string $attribute, $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();
        if (!$user instanceof User) {
            return false;
        }

        return match ($attribute) {
            self::VIEW, self::EDIT, self::DELETE, ... => 
                $user === $subject->getUser(),  // 核心检查：用户必须是条目所有者
            default => false,
        };
    }
}
```

**单条目权限核心逻辑**：`$user === $entry->getUser()`

### 5.2 权限检查流程

```
请求到达控制器
    ↓
#[IsGranted] 注解触发权限检查
    ↓
Symfony 调用 Voter 链
    ↓
MainVoter：集合级操作（LIST_ENTRIES, CREATE_ENTRIES...）
    → 检查 ROLE_USER
    ↓
EntryVoter：单条目操作（VIEW, EDIT, DELETE...）
    → 检查 user === entry.getUser()
    ↓
权限通过 → 执行业务逻辑
权限拒绝 → 返回 403 Forbidden
```

### 5.3 API 层权限检查示例
**文件位置**：`src/Controller/Api/EntryRestController.php`

```php
// 集合级：列出条目
#[Route(path: '/api/entries.{_format}', name: 'api_get_entries')]
#[IsGranted('LIST_ENTRIES')]
public function getEntriesAction(Request $request, EntryRepository $entryRepository)
{
    $pager = $entryRepository->findEntries($this->getUser()->getId(), ...);
    // 注意：Repository 层通过 userId 过滤，只返回当前用户的条目
}

// 单条目：查看
#[Route(path: '/api/entries/{entry}.{_format}', name: 'api_get_entry')]
#[IsGranted('VIEW', subject: 'entry')]
public function getEntryAction(Entry $entry)
{
    return $this->sendResponse($entry);
}

// 单条目：删除（列表操作中的额外检查）
if (false !== $entry && $this->authorizationChecker->isGranted('DELETE', $entry)) {
    $entityManager->remove($entry);
}
```

### 5.4 数据层面的权限保障

除了 Voter 层的权限检查，Repository 层也通过 userId 进行数据过滤：

```php
// 在 EntryRepository 中
public function findEntries($userId, ...)
{
    $qb = $this->createQueryBuilder('e')
        ->andWhere('e.user = :userId')
        ->setParameter('userId', $userId);
    // ...
}
```

**双重保障机制**：
1. Voter 层：检查用户是否有权限操作该条目
2. Repository 层：查询时只返回当前用户的条目

---

## 六、客户端撤销后存量令牌的处理

### 6.1 级联删除机制

Wallabag 通过 **Doctrine 级联操作** 和 **数据库外键约束** 双重机制确保令牌被正确清理。

#### 6.1.1 ORM 层面的级联删除
**文件位置**：`src/Entity/Api/Client.php:36-40`

```php
#[ORM\OneToMany(targetEntity: RefreshToken::class, mappedBy: 'client', cascade: ['remove'])]
protected $refreshTokens;

#[ORM\OneToMany(targetEntity: AccessToken::class, mappedBy: 'client', cascade: ['remove'])]
protected $accessTokens;
```

当删除 Client 实体时，Doctrine 会自动删除关联的 AccessToken 和 RefreshToken。

#### 6.1.2 数据库层面的级联删除
**文件位置**：`src/Entity/Api/AccessToken.php:22-24`

```php
#[ORM\JoinColumn(name: 'user_id', referencedColumnName: 'id', onDelete: 'CASCADE')]
protected $user;
```

当用户被删除时，数据库会自动级联删除该用户的所有令牌。

### 6.2 客户端删除流程
**文件位置**：`src/Controller/Api/DeveloperController.php:73-93`

```php
#[Route(path: '/developer/client/delete/{id}', name: 'developer_delete_client')]
public function deleteClientAction(Request $request, Client $client, ...)
{
    // 1. CSRF 检查
    if (!$this->isCsrfTokenValid('delete-client', $request->request->get('token'))) {
        throw new BadRequestHttpException('Bad CSRF token.');
    }

    // 2. 权限检查：只能删除自己的客户端
    if (null === $this->getUser() || $client->getUser()->getId() !== $this->getUser()->getId()) {
        throw $this->createAccessDeniedException('You can not access this client.');
    }

    // 3. 删除客户端（级联删除所有关联令牌）
    $entityManager->remove($client);
    $entityManager->flush();
}
```

### 6.3 完整的级联删除链

```
删除 Client
    ↓
Doctrine cascade: ['remove']
    ↓
删除所有关联的 AccessToken
删除所有关联的 RefreshToken
    ↓
数据库中 oauth2_access_tokens 表对应记录被删除
数据库中 oauth2_refresh_tokens 表对应记录被删除
    ↓
令牌立即失效（因为令牌已不存在于数据库）
```

### 6.4 用户删除时的令牌清理
**文件位置**：`src/Entity/User.php:129`

```php
#[ORM\OneToMany(targetEntity: Client::class, mappedBy: 'user', cascade: ['remove'])]
protected $clients;
```

用户删除时的级联链：
```
删除 User
    ↓
级联删除 User 的所有 Client
    ↓
每个 Client 级联删除其 AccessToken 和 RefreshToken
    ↓
同时，AccessToken/RefreshToken 的 user_id 外键 ON DELETE CASCADE
    ↓
所有相关令牌被彻底清理
```

### 6.5 令牌失效的验证

当使用已被删除的令牌访问 API 时：
1. FOSOAuthServerBundle 从数据库查询该 token
2. token 不存在，认证失败
3. 返回 401 Unauthorized

**注意**：没有单独的令牌撤销接口，只能通过删除客户端或用户来实现令牌撤销。

---

## 七、接口层与 Web 层权限逻辑对比

### 7.1 架构对比

| 对比项 | API 层（/api/.*） | Web 层（/.*） |
|--------|------------------|---------------|
| **防火墙** | `api` | `main` |
| **认证方式** | OAuth2 Bearer Token | Session Cookie |
| **状态** | 无状态（stateless） | 有状态 |
| **安全组件** | FOSOAuthServerBundle | FOSUserBundle + Symfony Security |
| **CSRF 保护** | 不需要 | 需要（表单提交） |

### 7.2 防火墙配置对比

#### API 层防火墙
**文件位置**：`app/config/security.yml:29-34`

```yaml
api:
    pattern: /api/.*
    fos_oauth: true          # OAuth2 认证
    stateless: true          # 无状态
    anonymous: true          # 允许匿名访问（部分端点）
    provider: fos_userbundle
```

#### Web 层防火墙
**文件位置**：`app/config/security.yml:40-61`

```yaml
main:
    pattern: ^/
    form_login:              # 表单登录
        provider: fos_userbundle
        csrf_token_generator: security.csrf.token_manager
    anonymous: true
    remember_me:             # Remember Me 功能
        secret: "%env(APP_SECRET)%"
        lifetime: 31536000
    logout:
        path: /logout
        target: /
    two_factor:              # 双因素认证
        provider: fos_userbundle
```

### 7.3 权限检查机制对比

#### 相同点
1. **共享同一套 Voter 系统**：MainVoter 和 EntryVoter 同时服务于 API 层和 Web 层
2. **相同的权限注解**：都使用 `#[IsGranted]` 注解
3. **相同的权限判定逻辑**：
   - 集合级权限：检查 `ROLE_USER`
   - 单条目权限：检查 `$user === $entry->getUser()`

#### 不同点

| 对比项 | API 层 | Web 层 |
|--------|--------|--------|
| **认证方式** | Authorization: Bearer {token} | Session Cookie |
| **用户获取** | 通过 token 解析用户 | 通过 session 读取用户 |
| **CSRF 保护** | 无（无状态设计） | 所有 POST 请求需要 CSRF token |
| **匿名访问** | 部分端点允许（如 /api/version） | 登录页、注册页允许 |
| **登录方式** | OAuth2 密码模式/授权码模式 | 表单登录 + 可选 2FA |
| **登出方式** | 客户端丢弃 token | 调用 /logout 销毁 session |

### 7.4 代码示例对比

#### API 层示例
```php
// 不需要 CSRF
#[Route(path: '/api/entries/{entry}.{_format}', name: 'api_get_entry', methods: ['GET'])]
#[IsGranted('VIEW', subject: 'entry')]
public function getEntryAction(Entry $entry)
{
    return $this->sendResponse($entry);
}
```

#### Web 层示例
```php
// 需要 CSRF 检查
#[Route(path: '/delete/{id}', name: 'delete_entry', methods: ['POST'])]
#[IsGranted('DELETE', subject: 'entry')]
public function deleteEntryAction(Request $request, Entry $entry)
{
    // Web 层必须验证 CSRF token
    if (!$this->isCsrfTokenValid('delete-entry', $request->request->get('token'))) {
        throw new BadRequestHttpException('Bad CSRF token.');
    }
    
    $entityManager->remove($entry);
    $entityManager->flush();
    
    return $this->redirect($redirectUrl);
}
```

### 7.5 访问控制（access_control）对比

**文件位置**：`app/config/security.yml:62-78`

```yaml
access_control:
    # API 相关
    - { path: ^/api/(doc|version|info|user), roles: IS_AUTHENTICATED_ANONYMOUSLY }
    
    # Web 相关
    - { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY }
    - { path: ^/logout, roles: [IS_AUTHENTICATED_ANONYMOUSLY, IS_AUTHENTICATED_2FA_IN_PROGRESS] }
    - { path: ^/register, role: IS_AUTHENTICATED_ANONYMOUSLY }
    - { path: ^/settings, roles: ROLE_SUPER_ADMIN }
    - { path: ^/2fa, role: IS_AUTHENTICATED_2FA_IN_PROGRESS }
    - { path: ^/, roles: ROLE_USER }  # 默认需要 ROLE_USER
```

---

## 八、总结

### 8.1 鉴权流程全景图

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  客户端注册     │────▶│  令牌颁发       │────▶│  条目访问       │
│  DeveloperCtrl  │     │  FOSOAuthServer │     │  Voter 检查     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
          │                       │                       │
          ▼                       ▼                       ▼
   创建 Client 实体        生成 AccessToken        MainVoter 检查 ROLE_USER
   关联 User             生成 RefreshToken       EntryVoter 检查所有权
                          存储到数据库            Repository 按 userId 过滤
```

### 8.2 关键设计特点

1. **基于标准 OAuth2 协议**：使用成熟的 FOSOAuthServerBundle，支持多种授权模式
2. **细粒度权限控制**：通过 Voter 系统实现 14 种单条目操作权限
3. **双重安全保障**：Voter 权限检查 + Repository 数据过滤
4. **自动令牌清理**：级联删除确保客户端/用户删除后令牌立即失效
5. **API/Web 统一权限模型**：两套认证机制共享同一套权限判定逻辑

### 8.3 安全注意事项

1. **令牌存储**：Access Token 和 Refresh Token 以明文存储在数据库中，需确保数据库安全
2. **令牌生命周期**：通过环境变量配置，建议设置合理的过期时间
3. **CSRF 保护**：Web 层所有 POST 操作必须验证 CSRF Token
4. **客户端密钥**：`client_secret` 仅在创建时显示一次，后续无法查看

### 8.4 潜在改进点

1. **令牌单独撤销**：当前只能通过删除客户端来撤销令牌，缺乏单独的令牌撤销接口
2. **令牌审计**：缺乏令牌使用日志和审计功能
3. **作用域支持**：所有令牌拥有完整权限，缺乏细粒度的 scope 控制
