# Wallabag 网页保存处理管道分析

## 一、处理管道总览

Wallabag 保存网页时的处理管道由 **同步主流程** 和 **同步事件驱动后置处理** 两部分串联而成。图片本地化并非异步执行——`DownloadImagesSubscriber` 通过 Symfony 事件调度器同步监听 `entry.saved` 事件，在主流程的 `dispatch()` 调用处就地执行，尚未入库的条目不会触发图片处理。

整体流程：

```
用户请求保存
    ↓
[EntryController / ApiController / Import / Command]
    ↓
ContentProxy::updateEntry()
    ├─ 有传入 HTML → Graby::cleanupHtml()       [噪声剔除]
    └─ 无有效 HTML → Graby::fetchContent()       [正文抓取 + 噪声剔除]
    ↓
ContentProxy::stockEntry()                         [内容加工]
    ├─ 设置标题、域名、阅读时间
    ├─ 语言检测、预览图提取
    └─ 自动标签
    ↓
EntityManager::persist + flush (第一次)             [入库，获得 entry ID]
    ↓
dispatch(EntrySavedEvent)                          [派发事件，同步执行]
    ↓
DownloadImagesSubscriber::onEntrySaved()           [图片本地化]
    ├─ processHtml(): 下载正文图片，替换 HTML 中的 URL
    ├─ processSingleImage(): 下载预览图
    └─ persist + flush (第二次): 保存替换后的内容
```

### 为什么必须先入库再处理图片

`DownloadImages::processHtml()` 的第一个参数是 `$entryId`，它用于生成图片的本地存储路径：

```
{baseFolder}/{hashId[0]}/{hashId[1]}/{hashId}/{crc32(imageUrl)}.ext
```

其中 `hashId = crc32(entryId)`。如果条目尚未 flush，`$entry->getId()` 为 null，路径无法生成。因此整个管道的时序是：

1. **先 flush**——条目获得数据库自增 ID
2. **再 dispatch**——Subscriber 拿到有效 ID 后才能下载图片并写入磁盘
3. **再 flush**——将图片路径替换后的 HTML 更新回数据库

这也解释了 `AbstractImport::parseEntries()` 中的注释：*"entry.saved needs the entry to be persisted in db because it needs its id to generate images"*。

---

## 二、各阶段详细分析

### 2.1 正文抓取与噪声剔除

正文抓取与噪声剔除由 Graby 库在一次调用中同时完成，不存在先抓取、后去噪的分离步骤。

#### 两条路径

**路径 A：导入模式（已有 HTML）**

当导入场景已提供 HTML 时，仅调用 `cleanupHtml()` 做噪声剔除：

```php
if (!empty($content['html'])) {
    $content['html'] = $this->graby->cleanupHtml($content['html'], $url);
}
```

**路径 B：抓取模式（从 URL 抓取）**

当没有有效内容（`$content` 为空，或缺少 title/html/url 任一字段）且未禁用内容更新时，调用 `fetchContent()`，该操作包含 HTTP 请求、站点配置匹配、Readability 正文提取、噪声剔除和元数据提取。

```php
if ((empty($content) || false === $this->validateContent($content)) && false === $disableContentUpdate) {
    $fetchedContent = $this->graby->fetchContent($url);
    // ...
}
```

### 2.2 内容加工

抓取完成后，`stockEntry()` 方法将数据填入 Entry 实体：

- 原始 URL 更新（处理重定向）
- 域名提取
- 标题设置
- 正文存储 + 阅读时间计算
- HTTP 状态码、作者、发布日期、语言
- 预览图提取（优先取 Open Graph image，其次从正文 img 标签中取第一张）
- 自动标签

### 2.3 图片本地化

`DownloadImagesSubscriber` 同步监听 `entry.saved` 事件。源码中的 `@todo` 注释明确指出 *"If we want to add async download, it should be done in that method"*——说明当前实现是同步的，异步仅是未来计划。

处理流程：

1. 检查 `download_images_enabled` 开关（默认关闭）
2. 从正文 HTML 提取所有 `<img>` 的 `src` 和 `srcset` URL
3. 逐张下载，通过 GD 库重新生成图片（安全考虑），SVG 使用 sanitizer 清理
4. 将远程 URL 替换为本地路径
5. 下载预览图（单独处理）
6. 再次 persist + flush

---

## 三、失败兜底机制

### 3.1 ContentProxy 层兜底

**抓取结果为错误消息时保留导入内容**

当导入场景已有内容，而 Graby 抓取返回的是错误消息 HTML，则保留导入内容不覆盖：

```php
if (empty($content) || $fetchedContent['html'] !== $this->fetchingErrorMessage) {
    $content = $fetchedContent;
}
```

**内容为空时设置错误占位**

```php
if (empty($content['html'])) {
    $content['html'] = $this->fetchingErrorMessage;
    $entry->setNotParsed(true);
    if (!empty($content['description'])) {
        $content['html'] .= '<p><i>But we found a short description: </i></p>' . $content['description'];
    }
}
```

标记 `isNotParsed = true` 使得后续可通过 `wallabag:entry:reload --only-not-parsed` 命令批量重试。

### 3.2 各入口失败分支差异

各入口对 `ContentProxy::updateEntry()` 抛出异常的处理策略各不相同：

| 入口 | 异常处理策略 | 抓取失败后是否入库 | 失败后的行为 |
|------|-------------|-------------------|-------------|
| **Web 保存** (`EntryController::updateEntry`) | try-catch 吞掉异常，切换 flash 消息 | **是**，条目照常 persist + flush | 显示"保存失败"提示，但条目仍入数据库（内容为错误占位） |
| **API 创建** (`EntryRestController::postEntriesAction`) | try-catch 记录日志，不中断 | **是**，条目照常 persist + flush | 返回条目 JSON（含错误占位内容），HTTP 200 |
| **API 重载** (`EntryRestController::patchEntriesReloadAction`) | try-catch，返回 HTTP 304 | **否**，检测到内容等于错误消息则跳过 flush | 返回空 JSON + HTTP 304 |
| **Web 重载** (`EntryController::reloadAction`) | try-catch 切换消息 | **否**，检测到内容等于错误消息则跳过 flush 和 dispatch | 重定向回条目页，显示"重载失败" |
| **导入** (`AbstractImport::fetchContent`) | try-catch 记录日志，不中断 | **是**，条目照常 persist + flush | 当前条目可能内容为空，但不影响后续条目的导入 |
| **队列消费** (`AbstractConsumer::handleMessage`) | flush 时 try-catch，返回 false | flush 失败时**否**，消息重试 | 返回 false 触发消息队列重试机制 |
| **CLI 重载** (`ReloadEntryCommand`) | **无 try-catch** | 异常会导致命令中断 | 未捕获异常直接抛出，命令以非零退出码终止 |

关键差异总结：

- **保存类入口**（Web 保存、API 创建、导入）：失败也入库，保证用户不丢数据
- **重载类入口**（Web 重载、API 重载）：失败不覆盖，保护已有内容
- **队列消费**：失败触发重试，保证最终一致性
- **CLI 命令**：失败直接中断，适合人工介入排查

### 3.3 图片下载失败兜底

`DownloadImages::processSingleImage()` 在以下情况返回 `false` 并跳过该图片：

- 无法解析为绝对 URL
- HTTP 请求失败
- 响应状态码非 200
- 图片格式不在白名单（仅 jpeg/png/gif/webp/svg）
- GD 库重建图片失败
- SVG sanitizer 清理失败

`DownloadImagesSubscriber` 对返回值的处理：`false` 则不更新对应字段，保持原始远程 URL 不变。这意味着图片下载失败时，用户看到的仍是远程图片（而非本地化版本）。

---

## 四、核心设计特点

1. **正文抓取与噪声剔除不可分割**：Graby 在 `fetchContent()` 内部一步完成，不暴露中间态
2. **先入库后图片本地化的时序约束**：条目 ID 是图片存储路径的组成部分，必须先 flush 获得 ID
3. **图片本地化是同步事件处理**：当前通过 Symfony EventDispatcher 同步执行，源码中 @todo 标注了异步化计划
4. **保存 vs 重载的失败策略对立**：保存优先"不丢数据"，重载优先"不覆盖好数据"
5. **图片下载的优雅降级**：单张失败跳过，整体失败保留远程 URL，不会因图片问题导致条目不可用
