# Wallabag 网页保存处理管道分析

## 一、处理管道总览

Wallabag 保存网页时的处理管道由 **同步主流程** 和 **同步事件驱动后置处理** 两部分串联而成。图片本地化并非异步执行——`DownloadImagesSubscriber` 通过 Symfony 事件调度器同步监听 `entry.saved` 事件，在主流程的 `dispatch()` 调用处就地执行，尚未入库的条目不会触发图片处理。

整体流程：

```
用户请求保存
    ↓
[EntryController / ApiController / Import / Command]
    ↓
ContentProxy::updateEntry()
    ├─ 有传入 HTML → Graby::cleanupHtml()       [噪声剔除]
    └─ 无有效 HTML → Graby::fetchContent()       [正文抓取 + 噪声剔除]
    ↓
ContentProxy::stockEntry()                         [内容加工]
    ├─ 设置标题、域名、阅读时间
    ├─ 语言检测、预览图提取
    └─ 自动标签
    ↓
EntityManager::persist + flush (第一次)             [入库，获得 entry ID]
    ↓
dispatch(EntrySavedEvent)                          [派发事件，同步执行]
    ↓
DownloadImagesSubscriber::onEntrySaved()           [图片本地化]
    ├─ processHtml(): 下载正文图片，替换 HTML 中的 URL
    ├─ processSingleImage(): 下载预览图
    └─ persist + flush (第二次): 保存替换后的内容
```

### 为什么必须先入库再处理图片

`DownloadImages::processHtml()` 的第一个参数是 `$entryId`，它用于生成图片的本地存储路径：

```
{baseFolder}/{hashId[0]}/{hashId[1]}/{hashId}/{crc32(imageUrl)}.ext
```

其中 `hashId = crc32(entryId)`。如果条目尚未 flush，`$entry->getId()` 为 null，路径无法生成。因此整个管道的时序是：

1. **先 flush**——条目获得数据库自增 ID
2. **再 dispatch**——Subscriber 拿到有效 ID 后才能下载图片并写入磁盘
3. **再 flush**——将图片路径替换后的 HTML 更新回数据库

这也解释了 `AbstractImport::parseEntries()` 中的注释：*"entry.saved needs the entry to be persisted in db because it needs its id to generate images"*。

---

## 二、各阶段详细分析

### 2.1 正文抓取与噪声剔除

正文抓取与噪声剔除由 Graby 库在一次调用中同时完成，不存在先抓取、后去噪的分离步骤。

#### 两条路径

**路径 A：导入模式（已有 HTML）**

当导入场景已提供 HTML 时，仅调用 `cleanupHtml()` 做噪声剔除：

```php
if (!empty($content['html'])) {
    $content['html'] = $this->graby->cleanupHtml($content['html'], $url);
}
```

**路径 B：抓取模式（从 URL 抓取）**

当没有有效内容（`$content` 为空，或缺少 title/html/url 任一字段）且未禁用内容更新时，调用 `fetchContent()`，该操作包含 HTTP 请求、站点配置匹配、Readability 正文提取、噪声剔除和元数据提取。

```php
if ((empty($content) || false === $this->validateContent($content)) && false === $disableContentUpdate) {
    $fetchedContent = $this->graby->fetchContent($url);
    // ...
}
```

### 2.2 内容加工

抓取完成后，`stockEntry()` 方法将数据填入 Entry 实体：

- 原始 URL 更新（处理重定向）
- 域名提取
- 标题设置
- 正文存储 + 阅读时间计算
- HTTP 状态码、作者、发布日期、语言
- 预览图提取（优先取 Open Graph image，其次从正文 img 标签中取第一张）
- 自动标签

### 2.3 图片本地化

`DownloadImagesSubscriber` 同步监听 `entry.saved` 事件。源码中的 `@todo` 注释明确指出 *"If we want to add async download, it should be done in that method"*——说明当前实现是同步的，异步仅是未来计划。

处理流程：

1. 检查 `download_images_enabled` 开关（默认关闭）
2. 从正文 HTML 提取所有 `<img>` 的 `src` 和 `srcset` URL
3. 逐张下载，通过 GD 库重新生成图片（安全考虑），SVG 使用 sanitizer 清理
4. 将远程 URL 替换为本地路径
5. 下载预览图（单独处理）
6. 再次 persist + flush

---

## 三、失败兜底机制

### 3.1 ContentProxy 层兜底：两条失败分支

`updateEntry()` 的失败有两种性质完全不同的分支，这是之前分析中容易混淆的关键：

#### 分支 A：Graby 正常返回但内容为空（占 99%+ 场景）

Graby 内部对所有 HTTP 异常做了 try-catch，失败时不会抛异常，而是返回一个结果数组，其中 `html` 字段为 `false` 或空字符串。单元测试的 mock 也验证了这一点——`fetchContent()` 返回 `['html' => false, 'title' => '', ...]`。

此时流程继续向下执行：`stockEntry()` 正常被调用，其中对空 html 做了兜底：

```php
if (empty($content['html'])) {
    $content['html'] = $this->fetchingErrorMessage;  // 写入错误占位符
    $entry->setNotParsed(true);                       // 标记解析失败
    if (!empty($content['description'])) {
        $content['html'] .= '<p><i>But we found a short description: </i></p>' . $content['description'];
    }
}
$entry->setContent($content['html']);  // content = 错误占位符字符串
```

**落库结果**：`content` 字段 = 配置的错误占位符字符串，`isNotParsed = true`，其余字段按 `stockEntry()` 的逻辑正常填充。

#### 分支 B：updateEntry() 真的抛出异常（极端边缘场景）

只有 `cleanupHtml()`（导入时调用）或 `fetchContent()` 中的深层代码（Graby 未兜住的异常）才会让 `updateEntry()` 抛异常。此时 `stockEntry()` 位于 `updateEntry()` 的最后一行，异常抛出后它**不会被执行**。

**落库结果**：取决于入口层是否有 try-catch 兜住——如果兜住，content 字段保持对象构造时的默认值 `null`；如果没兜住，请求直接中断。

#### 抓取结果为错误消息时保留导入内容

无论是分支 A 还是分支 B，当导入场景已有内容，而 Graby 返回的是错误消息 HTML 时，都会保留导入内容不被覆盖：

```php
if (empty($content) || $fetchedContent['html'] !== $this->fetchingErrorMessage) {
    $content = $fetchedContent;
}
```

标记 `isNotParsed = true` 使得后续可通过 `wallabag:entry:reload --only-not-parsed` 命令批量重试。

### 3.2 API 单条保存 vs 批量保存的失败分支差异

这两个入口看似相似，实则在异常处理和请求中断行为上有本质区别。

#### API 单条保存 (`postEntriesAction`)

```
contentProxy->updateEntry()    ← try-catch 包裹
    ↓ (无论是否异常，都继续执行)
设置 archive/star/tags/origin_url/isPublic 等业务字段
    ↓
validator->validate($entry)    ← 仅校验 url 字段（content 无约束）
    ↓
persist + flush                ← 入库
dispatch(EntrySavedEvent)
    ↓
返回条目 JSON，HTTP 200
```

**关键行为拆解**：

1. **异常捕获后 content 字段的最终值**：
   - 99% 场景（分支 A：Graby 返回空内容）：content = **错误占位符字符串**（stockEntry 正常写入）
   - 极端场景（分支 B：updateEntry 抛异常被 catch）：content = **null**（stockEntry 未执行，Entry 实体默认值）
   - 不存在 content = "空字符串" 的情况，Entry 实体 `content` 字段没有非空约束，默认值是 null

2. **Validator 的实际作用**：
   - Entry 实体只有 `url` 字段有 `#[Assert\NotBlank]` 和 `#[Assert\Url]` 约束
   - `content` 字段**没有任何 Assert 约束**，nullable=true
   - 所以 Validator 永远不会因为 content 为空或 null 而拦截
   - Validator 校验失败通常是 url 格式问题，此时抛 `BadRequestHttpException`，**不入库**

3. **兜底修补**：
   - `setDefaultEntryTitle()` 用 URL 的 basename 或主机名补齐 title
   - `setEntryDomainName()` 从 URL 解析域名补齐 domainName
   - content 字段（分支 B 时为 null）**没有兜底机制**，会原样入库

#### API 批量保存 (`postEntriesListAction`)

```
foreach ($urls as $key => $url):
    findByUrlAndUserId
    ↓
    if 新条目:
        contentProxy->updateEntry()   ← 无 try-catch！
    ↓
    persist + flush                   ← 入库
    dispatch(EntrySavedEvent)
    ↓
继续下一个 URL
```

**关键行为**：
- `updateEntry()` **无 try-catch 包裹**，异常会直接向上抛出
- 一旦触发（Graby 未兜住的深层异常或 cleanupHtml 抛异常），**整个请求中断**，后续所有 URL 都不会处理
- 已成功 flush 的条目不受影响（已入数据库），但当前条目和剩余条目全部丢失
- 无 Validator、无业务字段设置、无兜底逻辑——所有内容直接来自 `updateEntry()`

#### 两者对比

| 对比维度 | API 单条保存 | API 批量保存 |
|---------|------------|------------|
| updateEntry 异常处理 | try-catch 吞掉，记录日志 | **无 try-catch，异常直接抛出** |
| 异常后是否入库 | **是**（url 有效时 Validator 一定通过） | **否**，当前条目不入库，请求中断 |
| 是否中断请求 | 否，返回 HTTP 200 | **是**，返回 500 错误 |
| 对剩余条目的影响 | 不适用 | 后续 URL 全部跳过 |
| Graby 返回空内容时 content | **错误占位符字符串**（stockEntry 写入） | 同左 |
| updateEntry 真抛异常时 content | **null**（stockEntry 未执行） | 请求中断，不落库 |
| Validator 拦截 | url 格式问题时拦截，content 从不拦截 | 无 Validator |
| 兜底逻辑 | setDefaultEntryTitle + setEntryDomainName | 无 |
| 业务字段 | archive/star/tags/isPublic/origin_url | 无 |

注意：由于 Graby 内部几乎兜住了所有异常，批量保存的"一条失败全部中断"在实际中触发概率较低——只有当 Graby 内部未兜住的深层异常或 `cleanupHtml()` 出问题时才会发生。

### 3.3 所有入口失败时的内容写入策略

各入口失败时，Entry 对象中的 content 字段有且仅有四种可能状态：

| 状态 | content 值 | 触发条件 |
|------|-----------|---------|
| **错误占位符** | 配置的 fetchingErrorMessage 字符串 | Graby 返回空内容，stockEntry 正常执行（占绝大多数） |
| **null** | null（PHP 默认值） | updateEntry 真抛异常被 catch，stockEntry 未执行，且入口层有兜底让其入库 |
| **保留旧值** | 数据库中已有内容 | 重载场景检测到内容等于错误占位后跳过 flush |
| **不落库** | —— | Validator 拦截 / 异常未 catch 直接中断 / 重载跳过 |

各入口的具体策略：

| 入口 | Graby 返回空内容时的 content | updateEntry 抛异常时的 content | 是否入库 |
|------|---------------------------|-------------------------------|---------|
| **Web 保存** | 错误占位符（99%） | null（极端时 try-catch 兜住） | 是 |
| **Web 重载** | 保留旧值（检测到错误占位后跳过 flush） | 保留旧值（异常被 catch 后同样跳过 flush） | 否 |
| **API 单条保存** | 错误占位符（99%） | null（极端时 try-catch 兜住） | 是（url 有效时） |
| **API 批量保存** | 错误占位符（99%） | 不落库（异常未 catch，请求中断） | 异常时否 |
| **API 重载** | 保留旧值（返回 304） | 保留旧值（异常时返回 304） | 否 |
| **导入** | 错误占位符（99%） | null（try-catch 兜住，但无 content 兜底） | 是 |
| **队列消费** | 错误占位符（99%） | null（flush 时 catch，返回 false 重试） | flush 失败时否 |
| **CLI 重载** | 错误占位符（99%） | 不落库（异常未 catch，命令中断） | 异常时否 |

**null content 的隐患**：Web 保存、API 单条保存、导入这三个入口，在 updateEntry 真抛异常被 catch 的极端情况下，入库的 content 字段是 null。用户界面上正文区域将是空白——没有错误占位符提示，也没有 `isNotParsed=true` 标记，导致后续 `--only-not-parsed` 的批量重试命令也无法覆盖到这类条目。

### 3.4 图片下载失败兜底

`DownloadImages::processSingleImage()` 在以下情况返回 `false` 并跳过该图片：

- 无法解析为绝对 URL
- HTTP 请求失败
- 响应状态码非 200
- 图片格式不在白名单（仅 jpeg/png/gif/webp/svg）
- GD 库重建图片失败
- SVG sanitizer 清理失败

`DownloadImagesSubscriber` 对返回值的处理：`false` 则不更新对应字段，保持原始远程 URL 不变。这意味着图片下载失败时，用户看到的仍是远程图片（而非本地化版本）。

---

## 四、核心设计特点

1. **正文抓取与噪声剔除不可分割**：Graby 在 `fetchContent()` 内部一步完成，不暴露中间态
2. **先入库后图片本地化的时序约束**：条目 ID 是图片存储路径的组成部分，必须先 flush 获得 ID
3. **图片本地化是同步事件处理**：当前通过 Symfony EventDispatcher 同步执行，源码中 @todo 标注了异步化计划
4. **Graby 失败 vs updateEntry 异常是两层概念**：前者返回错误占位符（占 99%+），后者导致 content=null（极端边缘）
5. **Validator 只拦 URL 不拦内容**：content 字段没有 Assert 约束，异常被 catch 后只要 url 合法就一定入库
6. **保存 vs 重载的失败策略对立**：保存优先"不丢数据"（写入错误占位或 null 也要入库），重载优先"不覆盖好数据"（检测到异常即跳过 flush）
7. **单条保存 vs 批量保存的容错差距**：单条有 try-catch + Validator + 兜底；批量对 Graby 深层异常零容错，一条失败全部中断
8. **null content 的隐蔽缺陷**：updateEntry 极端异常被 catch 后入库的 content 为 null，无 isNotParsed 标记，无法被批量重试覆盖
